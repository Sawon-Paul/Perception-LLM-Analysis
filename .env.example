# cp .env.example .env   then put real values in .env
# .env is gitignored. NEVER commit it.

DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
OVERPASS_URL=https://overpass-api.de/api/interpreter
OVERPASS_CONTACT=your.email@example.com

# Folder holding all the PVS trace folders (PVS 1 ... PVS 9).
# FORWARD slashes. No quotes. No spaces around the '='.
DATASET_DIR=C:/Sawon/LLM/Dataset

# Which trace to use when no --trace is passed.
DEFAULT_TRACE=PVS 2
